import { SimulationParameters } from './simulation-parameters';

describe('SimulationParameters', () => {
  it('should create an instance', () => {
    expect(new SimulationParameters()).toBeTruthy();
  });
});
